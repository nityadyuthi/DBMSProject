<%- include("../partials/header") -%>
<form action="/modelParts/create" method="POST">
    <div class="padding row form-group">
        <div class="col-md-4">
            <label for="id">
                <h3>Model Name</h3>
            </label>
            <h4>
                <select class="btn btn-secondary white-text dropdown-toggle" name="modelID" id="modelID">
                    <% for (const i in data1) { %>
                    <option class="dropdown-item white-text font-semibold" value="<%= data1[i].ModelID %>">
                        <%= data1[i].ModelName %> </option>
                    <% } %>
                </select>
            </h4>
        </div>
    </div>
    <div class="padding row form-group">
        <div class="col-md-4">
            <label for="phone">
                <h3>Part Name</h3>
            </label>
            <h4>
                <select class="btn btn-secondary white-text dropdown-toggle" name="PartID" id="PartID">
                    <% for (const i in data) { %>
                    <option class="dropdown-item white-text font-semibold" value="<%= data[i].PartID %>">
                        <%= data[i].PartName %> </option>
                    <% } %>
                </select>
            </h4>
        </div>
    </div>
    <button type="submit" class="padding btn btn-lg btn-primary">Go!</button>
</form>
<%- include("../partials/footer") -%>